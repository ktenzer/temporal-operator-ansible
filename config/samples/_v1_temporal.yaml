apiVersion: temporal.io/v1
kind: Temporal
metadata:
  name: temporal-sample
spec:
  instance_name: temporal-test
  num_shards: 512
  plugin_name: mysql
  driver_name: mysql
  db_conn_addr: temporaltest-db.ce1n9s2ehuqa.us-east-1.rds.amazonaws.com:3306
  db_user: admin
  temporal_db_password: temporal
  admintools:
    enable: true
  web:
    enable: true
  frontend:
    enable: true
  worker:
    enable: true
  history:
    enable: true
  matching:
    enable: true
